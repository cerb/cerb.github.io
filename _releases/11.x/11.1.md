---
title:  "In Development: 11.1"
excerpt: Cerb 11.1 is a major feature update in development during March 2025 with 31 improvements from community feedback.
summary: This page details the release of Cerb 11.1, a significant feature update
  in development as of March 31, 2025. The update introduces over 31 new features and 
  improvements, driven by community feedback.
date: 2025-03-28 12:00:00 -0800
permalink: /releases/11.1/
social_image_url: /assets/images/releases/11.1/11.1.png
jumbotron:
  title:  "In Development: 11.1"
release:
  type: feature upgrade
  count: 31
---

<div class="cerb-screenshot">
<img src="{{page.social_image_url}}" class="screenshot" width="500">
</div>

**Cerb (11.1)** is a **{{ page.release.type }}** in development as of {{page.date|date:'%B %d, %Y'}}.  It includes more than **{{ page.release.count }}** new features and improvements from community feedback.

* TOC
{:toc}

<div class="cerb-box note">
	<p>
		To check if you qualify for this release as a free update, view <b>Setup &raquo; Configure &raquo; License</b>. If your software updates expire on or after <b>March 31, 2025</b> then you can upgrade without renewing your license.  Cerb Cloud subscribers will be upgraded automatically.
	</p>
</div>

{% comment %}
# Introduction
...

Thanks for supporting Cerb!

- Jeff Standen, Software Architect, Cerb
{% endcomment %}

# Important Release Notes

* [**Always make a backup before upgrading!**](/docs/backups/)

* **Cerb 11.1** requires **PHP 8.2+** and **MySQL 8.0+** (or **MariaDB 10.5+**).

* To upgrade your installation, [follow these instructions](/docs/upgrading/).

* **NOTE:** The [branches](https://github.com/cerb/cerb-release/branches) in the `cerb/cerb-release` repository have changed. The `master` branch has been removed, and new branches exist for each major version (e.g. `v11.0`, `v11.1`).

{% comment %}

# Identity Management

{% comment %}
* Identity management for portals. There are new 'identity' records that are organized into 'identity pools'. Identities are unique per pool, but can be duplicated between. Customer-facing portals and bots use identities for authentication/profiles rather than contacts. Teams with portals for different brands can decide if they want distinct identity pools or shared ones, in any combination.
{% endcomment %}

Identity management provides authentication and segmentation of public users in portals.

For example, portals and website bots may provide login functionality to authenticate a user before disclosing sensitive account information.

Previously, the Support Center portal used contact records for authentication; but this had several major limitations:

* Portals required contact records. To create a portal that used an alternative login method, a complete login system had to be built from scratch.

* There was no way to segment contacts so only certain users could access a particular branded portal. Similarly, there was no way to create separate identities with the same email address in different portals. This was particularly an issue in environments with multiple brands in a single Cerb instance. A client of one portal/brand was automatically assumed to be a client of all portals/brands.

* A contact record could have an unwieldy number of custom fieldsets when they interacted with multiple portals.

* Contacts could modify their account details which may conflict with changes made by workers.

* A contact can only be associated with a single organization.

Identity management addresses these issues by decoupling identity from the contact list.

Identities are organized into identity pools, and an identity (e.g. email/username) must be unique within the same pool, but may be duplicated between pools. This approach offers a lot of flexibility based on a team's specific needs.

Portals now inherently support identity-based sessions. This makes it very simple to create a new portal that authenticates using an identity pool. Previously this had to be built from scratch.

For instance, a company offering two unrelated products could have a separate identity pool for each product. The support portal for each product would use their own identity pool to register and authenticate users. A different company may offer several related portals that all share the same identity pool.

# Identity Portals

Identity portals. With identity pools we can now act as an OpenID Connect (OIDC) enabled identity provider (IdP). Portals and other scripts can defer to an identity portal to handle login/authentication/registration/verification/recovery/profile functionality. This drastically simplifies tooling. Identity portals themselves can defer to other SAML/OAuth/LDAP/AD providers (e.g. G Suite, Azure AD, GitHub, Salesforce), and auto-import (provision) those identities within an identity pool. Our bots and portals always deal with 'identity' records and have that complexity abstracted away.

# Portal Builder

Portal builder. One of the biggest strengths of Cerb is the fully customizable workspaces/dashboards/profiles. We're bringing this to customer-facing community portals with the 'Portal Builder', which allows arbitrary portal pages/dashboards/widgets to be assembled into new customer-facing tools (e.g. helpdesk, account control panel, discussion forums, comments widget for blog/docs). This is designed how we would have approached Cerb if we started in 2019. We rely on data queries, sheets, and form interactions to do most of the heavy lifting. This is capable of reproducing everything we did in the Support Center in a fully customizable way; as well as rectifying all of the shortcomings of the Support Center. For instance, we could have 'Licenses' and 'Cerb Cloud' pages that show up for logged-in identities with those resources. Those pages could list licenses and subscriptions using a sheet. Clicking into those can display a fully customizable profile page (i.e. whatever information you want to expose to the client). Those profile pages are essentially just dashboards anchored to a specific record. They can use sheets to easily display information, and form interactions to provide actions (e.g. renew, change seats, cancel).

* [Portal Builder]
  [[#612](https://github.com/jstanden/cerb/issues/612)]

- custom pages, custom widgets
- identity sessions / SSO
- custom CSS/footer
- modern (data queries/sheets)
- no jquery
- responsive/mobile

# Changelog

## Unreleased

* [Composer/Dependencies] Added the `steverhoades/oauth2-openid-connect-server` library for implementing OpenID server functionality.

* [Identities] Identity management provides authentication and segmentation of public users. For example, community portals and bots may provide login functionality to authenticate a user before disclosing sensitive account information. Previously, the Support Center portal used contact records for authentication; but this had several major limitations: there was no way to segment contacts so only certain users could access a particular branded portal, there was no way to create separate identities with the same email address in different portals, contacts could modify their account details which may conflict with changes made by workers, etc. Identity management addresses these issues by decoupling identity from the contact list. Identities are organized into identity pools, and an identity must be unique within the same pool, but may be duplicated between pools. This approach offers a lot of flexibility based on a team's specific needs. For instance, a company offering two unrelated products could have a separate identity pool for each product. The portal for each product would use their own identity pool to register and authenticate users. Another company may offer several portals that all share the same identity pool.

* [Identity Pools] Implemented identity pools for segmenting identities.

* [Portals/Identity] Portals now inherently support identity-based sessions. This makes it very simple to create a new portal that authenticates using an identity pool. Previously this had to be built from scratch.

* [Portal Builder]

* [Portal Builder/Pages] Implemented pages in the portal builder.

* [Portal Builder/Widgets] Implemented widgets in the portal builder.

* [Portal Builder/Dashboards] Implemented 'Dashboard' pages in the portal builder.

* [Portal Builder/Pages/Text] Implemented 'Text' pages in the portal builder. This page type displays arbitrary text, like a welcome page or privacy policy.

* [Portal Builder/Widgets/Text] Implemented 'Text' widgets in the portal builder.

* [WIP] identity portals

* [WIP] portal builder
{% endcomment %}
 
## Added

* **[Automations/LLM]** In automations, added an [llm.agent:](/docs/automations/commands/llm.agent/) command to simplify integrations with generative large language models (LLMs). The agent automatically manages history and tool use. Tools can be created using `llm.tool` automations with automatic documentation based on `inputs:`. The `llm.agent:` command will automatically invoke tools and return their output to the model. Integration is included for Anthropic, OpenAI, Groq, and Ollama.

* **[Automations/LLM]** In the `llm.agent:` automation command, non-automation `tools:` can be defined as type `tool:` with a description and parameters. When these tools are used by a model, the `on_tool:` event is triggered with custom logic and a `__tool` placeholder object with keys `id`, `name`, and `parameters`. A result must be returned using the `tool.return:` command. For example, this can use `await:interaction:` to delegate to any interaction, or to implement "human in the loop" manual approval workflows.

* **[Automations/LLM]** Added an [llm.tool](/docs/automations/triggers/llm.tool/) automation trigger. A collection of these tools can be provided to an `llm.agent:` using a large language model (LLM) that supports function calling (tool use). The inputs of an `llm.tool` automation are used to automatically generate the JSON Schema expected by common models. For instance, tools can provide access to Cerb data or integrate with third party APIs.

* **[Interactions/Website]** In website interactions, added a new `llmTranscript` form element. In two lines of code, this automatically handles rendering a chat history from an LLM session ID, with copy-to-clipboard, thumbs up/down ratings, etc. Previously, this had to be tediously implemented using a `sheet` element.

* **[Automations/LLM]** In automations, the `llm.agent:` command now returns a `session_id` to its output key. This can be used with the `llmTranscript` form element to render the chat history.

* **[Automations/LLM]** In automations, refactored the `llm.agent:` command so all tool use (including automations) executes the `on_tool:` branch. The same `__tool` placeholders are available as for custom tools. This allows real-time updates to be returned to the user during tool use; resetting the 30-second HTTP request time limit. Long-running deep research is now possible.

* **[Devblocks/Platform/LLM]** Added an LLM service to the Devblocks platform. This interfaces with Large Language Model APIs for generative text (e.g. Q&A, summarization, classification, report generation, email drafts).

* **[Platform/LLM/Ollama]** Added an `ollama` provider to the LLM service. This can connect to locally hosted open-source large language models through the Ollama API.

* **[Platform/LLM/OpenAI]** Added an `openai` provider to the LLM service. This can connect to hosted large language models through the OpenAI API (e.g. `gpt-4o`). A customizable `api_endpoint_url` also allows the use of other compatible services.

* **[Platform/LLM/Groq]** Added a `groq` provider to the LLM service. This connects to fast, hosted, open-source large language models through the Groq API.

* **[Platform/LLM/Anthropic]** Added an `anthropic` provider to the LLM service. This connects to hosted large language models through the Anthropic API (e.g. `claude-3-5-sonnet-20241022`).

* **[Platform/LLM/HuggingFace]** Added a `huggingface` provider to the LLM service. This uses the Serverless Inference API by default, but Inference Endpoints are also supported.

* **[Platform/LLM/TogetherAI]** Added a `together` provider to the LLM service. This uses the together.ai inference cloud service.

* **[Automations/LLM]** Added an `llm.embedding` automation trigger. This interfaces with large language model APIs to produce vector embeddings for blocks of text. For instance, embedding knowledgebase articles and FAQs for semantic search.

* **[Automations/LLM]** Added an [llm.embed:](/docs/automations/commands/llm.embed/) command to automations. This runs `llm.embedding` automations to produce vector embeddings for blocks of text. For instance, embedding knowledgebase articles and FAQs for semantic search.

* **[Automations/Inputs]** In automations, each input can now set an optional `description:` key. This is used for documentation, autocompletion, and JSON Schema in LLM tool use.

* **[Automations/Inputs]** In automations, `text:` inputs can now set an optional `allowed_values:` key to create picklists. This is used for documentation, autocompletion, and JSON Schema in LLM tool use.

* **[Devblocks/Platform/LLM]** Added a 'DatabaseHistory' memory module to the LLM service for managing large language model chat history + context.

* **[Automations/LLM]** In automations, [llm.agent:](/docs/automations/commands/llm.agent/) transcripts are now viewable in **Setup->Developers->LLM Agent Transcripts**. Transcripts can be marked read or deleted. When an agent uses a tool, its parameters and results are shown in the transcript. This makes debugging and compliance much easier. It also provides valuable feedback for improving agents based on past user interactions.

* **[Automations/LLM]** In LLM Transcripts, a 'Permalink' button provides a direct link for sharing transcripts.

* **[Automations/LLM]** When viewing LLM agent chat transcripts, metadata is displayed at the top (e.g. provider, user, client IP, automation, and automation node).

* **[Automations/LLM]** When viewing LLM agent chat transcripts, added a 'Copy' button to copy the plaintext (Markdown) content from a message to the browser clipboard.

* **[Interactions/Website]** In [interaction.website](/docs/automations/triggers/interaction.website/) automations, `submit:` form elements can now specify the width `size:` of each button: whole, half, third, and quarter.

* **[Interactions/Website]** In [interaction.website](/docs/automations/triggers/interaction.website/) automations, `say:` form elements can now specify several `styles:` options: text-center, text-large, text-left, text-right, or text-small.

* **[Worklists/Search/Performance]** Improved the performance of many complex search queries by running some subqueries independently and merging by IDs. This is enabled by default and can be disabled from the `APP_OPT_SQL_SUBQUERY_TO_IDS` configuration option.

* **[Interactions/Website]** In website interactions, the `submit:` form element has a new `is_automatic@bool` option. When `true`, the form is automatically submitted after it is rendered. This is particularly useful before a time-intensive operation like LLM text generation, which will instantly transition to a waiting spinner.

## Changed

* **[Portals/Interactions]** In 'Website Interaction' portals and on external websites, interactions now start in 'full' mode and use a blur overlay on the underlying website. The previous 'popup' default style was difficult to see on some website themes.

* **[Worklists/Fieldsets]** In worklists, improved the performance of the `fieldset:` filter.

* **[Interactions/Website]** In website interactions, the `sheet:` form element now applies styling to code blocks in `markdown` columns.

* **[Setup/Plugins]** In the setup page, moved 'Plugins' from its own menu into the 'Configure' menu. This menu had a single item, and we can use the space for something else.

* **[Platform/Markdown]** In Devblocks, changed the Markdown parsing library from Parsedown to League\CommonMark. Parsedown has some rendering issues and appears to have been abandoned since 2019.

{% comment %}
## Deprecated

## Removed

## Fixed

## Security
{% endcomment %}